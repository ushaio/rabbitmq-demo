server:
  port: 8081

spring:
  application:
    name: redis-demo
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      # password: your_password
      database: 0
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
